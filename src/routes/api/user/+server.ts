// import { type RequestHandler } from '@sveltejs/kit';

// import type { MyLocals } from '$lib/types';

// export const GET: RequestHandler = async (event) => {
//   const userId = (event.locals as MyLocals).user;
//   console.log('ENTRA')
// };
